## 2026-04-13

- 入庫画面（既存ロット）で、ロット選択時に場所別在庫 API（GET /api/inventory/lots/{lot_id}/location-stocks）に基づき、現在本数・重量のある置き場のチェックボックスを初期選択するようにした（従来はローカルに保存した直近1棚のみが選ばれ、ロットを変えても置き場の選択が更新されないことがあった）。

## 2026-04-03

- 入庫画面で、総本数・総重量は1件のまま、置き場だけ複数棚をメモ的に指定できるようにした。取引に `location_note` を追加し、履歴でも複数棚を表示する。
- 出庫 API の「最古ロット以外からの出庫拒否」制約を撤廃。フロントから FIFO 表示・説明書きを削除。出庫・移動のデフォルトロットは、作成日昇順で並べた一覧の先頭（従来の「古い順」表示）のまま。
- ロット一覧（PUT `/api/lots/{id}`）で、編集先のロットコードが同一材料の別ロットですでに使われている場合、**編集中ロットの取引をその既存ロットへ付け替え**、編集中のロット行を削除して統合する。間違った入庫で作ったロットを、正しいロットコードに合わせて保存できるようにした（従来は UNIQUE 制約で 409）。

## 2026-04-02

- ロットコードの一意制約を「材料ごと」に変更（`UNIQUE(material_id, lot_code)`）。入庫で「新規ロット」に入力したコードが既存ロットと同じ場合は、新規行を作らず既存ロットを返す（残在庫 0 で一覧に出ないロットへの追加入庫に対応）。単価だけ異なる場合は既存ロットの単価を更新する。
- 入庫画面: 既存ロットを選んだときは、場所別在庫 API に基づき在庫のある棚を入庫先の初期候補に設定（別棚への入庫はプルダウンで変更可能）。
- 出庫画面: 同一ロットが複数棚に分かれて置かれる前提で、出庫元は「場所別在庫 API」に在庫のある棚のみを表示し、棚ごとの本数・重量を表示。戻し先は従来どおり全棚から選択。
- 保管場所（棚番固定）のため、閲覧専用だった保管場所マスタ画面 `/locations` とナビ「Loc」を削除した。`GET /api/stock-locations` と入出庫・移動画面のプルダウンは従来どおり。
- 保管場所（stock location）を追加。マスタ `stock_locations`、取引に `location_id` / 移動用 `location_from_id`・`location_to_id`、種別 `transfer`（場所間移動・ロット総量は不変）を導入。既存 DB は起動時マイグレーションで既定場所「未設定」に紐づける。
- API: GET/POST `/api/stock-locations`、GET `/api/inventory/lots/{lot_id}/location-stocks`、入出庫・戻しは `location_id`（省略時は既定場所）。フロントは入庫・出庫・移動画面と履歴表示を対応。
- 保管場所マスタ画面 `/locations`（ナビ「Loc」）と PUT/DELETE `/api/stock-locations/{id}` を追加。
- 置き場（保管場所）は棚番 **1〜299** の整数のみとし、API・画面で検証。旧「未設定」はマイグレーションで棚番1に寄せる。
- 起動時に `stock_locations` へ棚番1〜299を **INSERT OR IGNORE で全件** 登録。マスタ追加・削除・棚番変更は不可（表示順のみ PUT 可）。保管場所マスタ画面は閲覧用。
- トランザクション一覧 GET `/api/transactions` を `items` + `total` のページング形式に変更し、クエリ `q` でメモ・ロットコード・取引 ID の部分一致検索に対応した。
- ロット一覧 GET `/api/lots/{material_id}` を同様にページング化（`limit` 省略時は全件）。クエリ `q` でロットコード部分一致。
- GET `/api/inventory/{material_id}/lot-summaries` を追加（残在庫ロットのページング・ロットコード検索。`nonzero_only` で残本 0 を除外可能）。
- フロント: Home（ロット別在庫・最近の取引）、履歴、Lots の各一覧に検索と `PaginationBar` を追加。共通コンポーネント `frontend/src/lib/PaginationBar.svelte`。
- 登録済みロットのロットコード・単価を PUT `/api/lots/{id}` で後から修正できるようにした（重複するロットコードは 409）。
- 専用画面 `/lots`（ナビ「Lots」）でロット一覧を表示し、コード・単価を編集できるようにした。入庫画面・ダッシュボードのロット別在庫から当画面へリンク。

## 2026-03-31

- 出庫時の本数上限を帳簿上の本数ではなく、ロットごとの実効本数（帳簿本数と重量÷単重の換算の大きい方）で判定するようにした。残本数 0・残重量が単重以上ある場合でも、重量に見合う範囲で出庫できる。
- 在庫 API に `total_effective_quantity`（実効本数合計）と `weight_per_unit` を追加し、ダッシュボードの「本」表示は実効合計を優先するようにした。

## 2026-03-26

- 入出庫送信時の idempotency key 生成を `crypto.randomUUID()` 固定から後方互換ありの生成処理へ変更し、古い社内ブラウザでも登録が無反応になりにくいようにした。
- backend の CORS 設定を環境変数で拡張できるようにし、README に LAN 公開時の `--host 0.0.0.0` と `CORS_ALLOW_ORIGINS` 設定例を追記した。
- Backend の開発用待ち受けポートを 8020 に変更し、Frontend の開発プロキシと README の接続先表記をあわせて更新した。
- 材料在庫APIにロット別在庫サマリーを追加し、材料総数とロット残数を同時に取得できるようにした。
- 出庫時はロット在庫不足を検知し、最古の在庫ありロット以外からの出庫を拒否する FIFO 制約を追加した。
- ダッシュボード、出庫画面、履歴画面でロット別残数と FIFO 優先ロット、取引ロットコードを確認できるようにした。
- 起動時に既存データの負在庫を自動で補正する adjust 取引を追加し、現在庫金額は残重量と現行ロット単価から算出するようにした。
- POST /api/lots を追加し、入庫画面から既存ロット選択と新規ロット作成を切り替えて登録できるようにした。
- 社内サーバー上の NSSM 運用で入出庫時に待ちやすい事象への対策として、frontend API 呼び出しにタイムアウトを追加し、backend の SQLite 接続を WAL と busy_timeout 設定で強化した。
